apiVersion: v1
kind: Service
metadata:
  name: nodejs-demoapp
  namespace: prod    
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  selector:
    app: nodejs-demoapp